/**
 * Палитра дизайна «Хекслет Карьера».
 *
 * Значения взяты из прототипа `docs/design/prototypes/site.dc.html` — он
 * источник истины для публичного сайта. Держим их в одном месте, чтобы
 * секции лендинга не расходились по оттенкам: в прототипе рядом живут
 * несколько близких серых, и на глаз их путать легко.
 */
export const palette = {
  /** Фирменный синий: кнопки, акценты, активные состояния. */
  brand: '#116EF5',
  /** Синий при наведении и для текста на светло-синей подложке. */
  brandDark: '#0B5CD6',
  /** Светло-синяя подложка под иконки и бейджи. */
  brandSoft: '#EAF2FE',

  /** Основной тёмный: заголовки и тёмные карточки. */
  ink: '#17171E',
  /** Тёмный для текста внутри списков. */
  inkSoft: '#2A2C33',
  /** Основной серый для абзацев. */
  text: '#4A4F59',
  /** Серый для подписей и сносок. */
  muted: '#6A6F7A',
  /** Самый светлый серый: метаданные внутри карточек. */
  faint: '#9AA0AB',

  /** Подложка секций, чередующихся с белыми. */
  surface: '#F4F5F7',
  /** Граница карточек и разделители секций. */
  border: '#E6E8EC',
  /** Граница вторичных кнопок — темнее обычной. */
  borderStrong: '#D5D9E0',

  /** Зелёный: успех, офферы, «набор открыт». */
  green: '#2F9E44',
  /** Зелёный для текста на светло-зелёной подложке. */
  greenDark: '#2B8A3E',
  /** Светло-зелёная подложка. */
  greenSoft: '#E9F8EE',
  /** Оранжевый: предупреждение о непройденных тестах. */
  orange: '#E8590C',

  /** Фиолетовый акцент AI-функций — только на тёмных карточках. */
  aiSoft: 'rgba(103, 65, 217, 0.45)',
  /** Текст и иконки на фиолетовой подложке. */
  aiText: '#C5B3F5',

  /** Текст на тёмных карточках. */
  onInk: '#9AA2B4',
  /** Более светлый текст на тёмных карточках. */
  onInkBright: '#B9BECC',
} as const

/** Тени из прототипа: у карточек лендинга их три разновидности. */
export const shadows = {
  card: '0 12px 30px rgba(23, 23, 30, 0.10)',
  cardSoft: '0 12px 30px rgba(23, 23, 30, 0.06)',
  raised: '0 18px 44px rgba(23, 23, 30, 0.10)',
  dark: '0 14px 34px rgba(23, 23, 30, 0.28)',
  brand: '0 12px 32px rgba(17, 110, 245, 0.12)',
} as const
