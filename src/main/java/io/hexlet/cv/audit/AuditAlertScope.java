package io.hexlet.cv.audit;

/**
 * Размерность, по которой считалась аномалия.
 * Одна серия неудач попадает под обе: перебор с одного адреса виден по IP,
 * подбор пароля к одному аккаунту с разных адресов - только по SUBJECT.
 */
public enum AuditAlertScope {
    IP,
    SUBJECT
}
