package io.hexlet.cv.audit;

/**
 * Вид аномалии, о которой сообщает журнал.
 * Отделён от {@link AuditEventType}: аудит фиксирует произошедшее, алерт - вывод,
 * сделанный по серии событий, и читают эти строки по разным поводам.
 *
 * <p>Держит поле alert= типизированным наравне с остальными полями строки журнала:
 * словарь аномалий закрыт и перечислим, по нему строятся grep и правила оповещения.
 * Одно значение - сегодняшнее состояние, а не предел: детектор пока один
 * ({@link LoginFailureRateDetector}), список растёт вместе с детекторами.
 */
public enum AuditAlertType {
    LOGIN_FAILURE_SPIKE
}
